
const INITIAL_STATE = {
  expenses: [],
  income:[],
  net: 0
}

const finances = (state=INITIAL_STATE, {type, payload}) => {
  switch(type){
    default: 
      return state
  }
}

export default finances